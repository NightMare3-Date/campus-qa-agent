services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # 知识文档持久化（挂载外部目录）
      - ./knowledge:/app/knowledge
      # 向量索引持久化
      - faiss_data:/app/data/faiss_index
    environment:
      - HF_HUB_OFFLINE=0
      - VECTOR_DB_DIR=/app/data/faiss_index
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  faiss_data:
